/** @format */

// This just lets TypeScript know about SvelteKit's built-in types
// this file usually doesnt need to be touched, but you can add your own types here if you want
declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
